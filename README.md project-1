Student Management System is a backend application developed using Java Spring Boot that provides RESTful APIs to manage student data.
It allows operations like adding, updating, viewing, and deleting student records stored in a PostgreSQL database.
The project follows a layered architecture using Spring MVC, Spring Data JPA, and is structured for easy integration with a frontend like React.
